services:
  spotify-playlist-helpers:
    image: "${ECR_REPOSITORY_URL:?not set - copy .env.example to .env}:${IMAGE_TAG:-0.1-dev}"
    volumes:
      - ./config.json:/app/configs/config.json:ro
    ports:
      - "3000:3000"
